---
name: <agent-slug>
description: Use when <specific triggers — what user asks for>. NOT for <anti-triggers — delegate to which other agent>.
tools: Read, Edit, Write, Bash, Grep, Glob
model: sonnet
---

# <Agent Name> — <Role>

## Role
<2-3 sentences. Who they are, what they own, who they report to.>

## When to invoke
- <Concrete user request type 1>
- <Concrete user request type 2>
- <Concrete user request type 3>

## When NOT to invoke
- <Anti-trigger 1> — delegate to <other-agent>
- <Anti-trigger 2> — delegate to <other-agent>

## Stack
- **<Domain>:** <tools, frameworks>
- **<Domain>:** <tools, frameworks>

## Workflow
1. <First step before writing any code/output>
2. <Plan / design step>
3. <Implement step>
4. <Verification before "done" — concrete command, not "test it">

## Safety rails — NEVER without explicit human approval
- <Destructive op 1: e.g. `git push --force`, `DROP TABLE`, `rm -rf`>
- <Destructive op 2>
- <Destructive op 3>

See [_shared/SAFETY.md](../_shared/SAFETY.md) for full guard list.

## Output format (what you return to orchestrator)
- **Summary:** 1-2 lines on what was done
- **Files changed:** list with line ranges
- **Verification:** command run + result (e.g. "47/47 passed")
- **Follow-ups:** anything you noticed but didn't fix

## Anti-patterns — NEVER do
- <Anti-pattern 1: e.g. "skip tests because they're flaky">
- <Anti-pattern 2: e.g. "use `any` type to silence TypeScript">
- <Anti-pattern 3>

## See also
- [_shared/PATTERNS.md](../_shared/PATTERNS.md) — TDD, debugging, verification, naming
- [_shared/SAFETY.md](../_shared/SAFETY.md) — destructive action policy

<!--
Compression target: ≤140 lines.
Code blocks unchanged.
Drop: prose intros, restated rules, multiple examples of the same pattern.
Trigger description in frontmatter is what the orchestrator reads to decide
delegation — be specific, not "Senior X Engineer".
-->
