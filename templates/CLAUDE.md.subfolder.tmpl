# <FolderName>/ — Coordinator hint

Short, focused note for one folder. Auto-loaded by Claude Code on entry. Keep it under 30 lines.

## Lookup
- List in: `../manifest.json.<key>[]`
- Cross-cutting helpers: `<path-if-any>`

## File format
KMF (`type: <kind>`). Required frontmatter:
```yaml
---
id: <kind>-<slug>
type: <kind>
v: 1
tags: [...]
refs: [...]
updated: YYYY-MM-DD
---
```

Required sections: `## <H2_1>`, `## <H2_2>`, `## REFS`.

## When to add a new file
- <Specific trigger 1>
- <Specific trigger 2>

## After writing
`python3 ../scripts/regen-manifest.py` to register.

## Anti-patterns
! Don't:
- Duplicate information present in another folder — ref it instead
- Skip the frontmatter — manifest regeneration depends on it
- Commit secret values — use `@secrets:<key>` refs

---

**How to use this template:** copy into each child folder of your brain (`Agents/`, `Decisions/`, `Knowledge/`, `Conversations/`, `Homelab/` or equivalents). Replace placeholders with content specific to that folder.
