---
type: index
tags: [lookup]
updated: YYYY-MM-DD
---

# INDEX — flat lookup

Quick lookup instead of grepping 30 files. Update on every infra/config change.
Target: ≤100 lines.

## Hosts

| IP / hostname | Role | Notes |
|---------------|------|-------|
| 10.0.0.1 | gateway | — |

## Services

| Domain / URL | → IP:port | Notes |
|--------------|-----------|-------|
| example.com | 10.0.0.10:443 | TLS via cert-manager |

## Ports (per host)

- **10.0.0.10:** 80/443 nginx, 5432 postgres, 6379 redis

## Access

- SSH: `ssh user@host` (key path: `~/.ssh/id_ed25519`)
- DB: env var `DATABASE_URL` (loaded from `.env`, gitignored)

## Paths

- Memory: `~/.claude/projects/<project>/memory/`
- Brain root: `~/AI-Brain/`
- Decisions/ADR: `~/AI-Brain/Decisions/`

## Workflows

- Deploy: `<command>`
- Backup: `<location, schedule>`
- Monitoring: `<dashboard URL>`
